# Use /api when frontend and backend are served through the Docker nginx proxy.
# For Vercel, set this to the public backend URL, for example:
# https://skaut-api.example.com
VITE_API_BASE_URL=/api

# Optional until Google Maps is enabled.
VITE_GOOGLE_MAPS_API_KEY=
